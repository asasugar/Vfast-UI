# ESLint Config of Vfast

## Install

```shell
# with npm
npm i @vfast/eslint-config -D

# with yarn
yarn add @vfast/eslint-config -D

# with pnpm
pnpm add @vfast/eslint-config -D
```

## Usage

```json
{
  "extends": ["@vfast"]
}
```
