# Stylelint Config of Vfast

## Install

```shell
# with npm
npm i @vfast/stylelint-config -D

# with yarn
yarn add @vfast/stylelint-config -D

# with pnpm
pnpm add @vfast/stylelint-config -D
```

## Usage

```json
{
  "extends": ["@vant/stylelint-config"]
}
```
